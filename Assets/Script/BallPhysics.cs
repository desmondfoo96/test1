using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BallPhysics : MonoBehaviour {
  
    // Use this for initialization
    void Start () {
        //middle
       // transform.GetComponent<Rigidbody>().AddForce(new Vector3(0f, 0.7f, 1) * 35);
        //block top right
        //transform.GetComponent<Rigidbody>().AddForce(new Vector3(0.45f, 1.4f, 1) * 35);
        //top right
        //transform.GetComponent<Rigidbody>().AddForce(new Vector3(2.4f, 0.8f, 1) * 10);
    }
 
    // Update is called once per frame
    void Update () {
        
    }


    
}
